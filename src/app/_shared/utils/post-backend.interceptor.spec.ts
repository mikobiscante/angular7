import { PostBackendInterceptor } from './post-backend.interceptor';

describe('PostBackend', () => {
  it('should create an instance', () => {
    expect(new PostBackendInterceptor()).toBeTruthy();
  });
});
